@Event
script ManualFireEvent extends EventType
    -- 수동 개입 창 오픈 통지 (SYS-SKILLQUEUE §5). Logic 발신 — HUD가 조준 UI를 노출한다.
    -- directional: 방향 종속 스킬=4방향 버튼(1탭 즉시 발동) / 방향 무관=중앙 발동 버튼 1개.
    property string skillName = ""
    property boolean directional = true
end
